"""查询流程相关提示词模板 - 育儿领域"""

# =============================================================================
# 问题分析/检索路由
# =============================================================================

SEARCH_ROUTER_TEMPLATE = """你是一个育儿知识库的智能路由助手。请分析用户的问题，并提取关键信息。

【当前日期】
{current_date}

【历史对话】
{history_text}

【用户问题】
<user_query>
{query}
</user_query>

请严格遵守以下规则：
1. `rewritten_query`：改写用户问题，使其成为可独立理解的完整问题。
2. `age_group`：判断问题主要面向的年龄段，可选值：0-3岁 / 3-6岁 / 6-12岁 / 12+岁。无法判断则为空字符串。
3. `problem_type`：判断问题核心类型，可选值：情绪管理 / 行为引导 / 学习能力 / 社交能力 / 亲子沟通 / 睡眠习惯 / 饮食习惯 / 手足关系 / 入园适应 / 安全教育 / 青春期 / 其他。无法判断则为空字符串。
4. `query_domain`：判断问题领域，可选值：
   - `parenting`：育儿相关问题（情绪、行为、教育、健康、沟通、睡眠、饮食等）
   - `general`：一般知识问题
   - `out_of_domain`：与育儿无关的问题
5. `route_action`：
   - `retrieve`：需要检索知识库
   - `answer`：简单寒暄或已有足够信息可直接回答

请直接返回 JSON 格式：
{{
    "rewritten_query": "改写后的问题",
    "age_group": "3-6岁",
    "problem_type": "情绪管理",
    "query_domain": "parenting",
    "route_action": "retrieve"
}}
"""

# =============================================================================
# HyDE 假设性文档生成
# =============================================================================

USER_HYDE_PROMPT_TEMPLATE = """请围绕以下育儿主题，模拟一段育儿知识文档的内容，该内容能够回答用户问题。

育儿主题：{age_hint}{problem_hint}

用户问题：{rewritten_query}

要求：
1. 以育儿专家、育儿指南或育儿知识科普的口吻撰写，使用陈述句
2. 直接描述育儿方法、建议、沟通技巧和注意事项，不要出现"假设"、"可能"、"大概"等不确定用语
3. 包含具体的育儿策略、年龄段适用性、场景描述等关键词，保持回答与查询主题高度相关
4. 不需要标题和格式标记，只输出正文段落
5. 使用中文，300-350字
"""

# =============================================================================
# 最终答案生成
# =============================================================================

ANSWER_PROMPT = """你是一个育儿知识助手，请严格根据参考内容回答家长的育儿问题。

要求：
1. **只使用【参考内容】中明确出现的信息作答。** 严禁添加参考内容中没有的育儿建议、方法步骤或注意事项。
2. 回答开头使用"根据育儿知识库资料"。
3. 如果【参考内容】不足以完整回答，请说明"根据现有资料，我了解到..."并只给出参考内容中明确提到的部分；如果完全无法回答，请直接说明"暂未找到相关资料"，不要自行补充建议。
4. 回答应包含以下结构（如果参考内容中有相关内容）：
   - 问题分析：简要说明这个育儿问题的原因和背景
   - 建议和方法：具体的应对策略和步骤
   - 沟通话术：可参考的对话示例
   - 注意事项：需要避免的常见误区
5. **格式规范（严禁使用 # 号标题）：**
   - 板块标题使用加粗+emoji：`**🧩 问题分析**`、`**💡 建议和方法**`、`**💬 沟通话术**`、`**⚠️ 注意事项**`，禁止使用 `###` 或 `####` 等 markdown 标题标记
   - 列表项统一使用 `- ` 开头，每条建议单独一行，简洁有力
   - 沟通话术中的对话示例用 `> ` 引用块包裹，每条一行
   - 话术子标题也使用加粗：`**✅ 正向示范**` 和 `**❌ 应避免的说法**`
   - 各板块之间用一个空行隔开，不出现 `---` 分隔线
   - 正文与列表之间保留一个空行
6. **禁止以下行为：**
   - 从通用知识中补充参考内容未提及的育儿方法
   - 添加参考内容中没有的安全提示或医学建议
   - 编造参考内容中没有的案例或数据
   - 对参考内容中的简略描述进行过度展开

【当前日期】
{current_date}

【参考内容】
{context}

【历史对话】
{history}

【问题领域】
{query_domain}

【适用年龄段】
{age_group}

【问题类型】
{problem_type}

【检索来源】
{retrieval_source}

【用户问题】
<user_query>
{question}
</user_query>

请回答："""
